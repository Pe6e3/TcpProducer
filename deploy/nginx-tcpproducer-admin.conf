# Панель TcpProducer на https://антон.su/tcp/ (Punycode: xn--80a0aadt.su)
# Добавьте location-блоки из этого файла в /etc/nginx/sites-available/logexplain
# (см. deploy/nginx-logexplain-tcpproducer.conf)
#
# Сертификат Let's Encrypt уже выпущен:
#   /etc/letsencrypt/live/xn--80a0aadt.su/
#
# Проверка/продление:
#   certbot certificates
#   certbot renew --dry-run
